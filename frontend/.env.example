# Frontend Environment Variables Template
# Leave blank during local dev to use Vite dev proxy, or set to deployed backend server URL in production:
VITE_API_URL=https://skin-infinity-backend.onrender.com
